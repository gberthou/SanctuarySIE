#ifndef ITEMTYPE_H
#define ITEMTYPE_H

enum ItemType
{
    PICKUP,
    TRADABLE,
    PERMANENT,
    WEAPON,
    ARMOR,
    ACCESSORY,
    STUFF,
    CONSUMABLE
};

enum WeaponType
{
	DAGGER
};

enum ArmorType
{
    // insert some armor name here
};

enum AccessoryType
{
    BATFANG
};

enum PickUpItemType
{
    GOLD,
    MANA
};

#endif // ITEMTYPE_H
